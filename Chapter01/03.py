def run_timing():
    total_run_min = 0
    average_time = 0

    while True:
     run_min = float(input('Enter 10km run time:'))
     total_run_min += run_min
     average = total_run_min // run_min
     
     if not run_min:
         print(f'Average of'+ {run_min}/{run_mins}+',' + 'over {run_mins} runs')

run_timing()


       
       